import { TimelineConfiguration } from './timeline-configuration';

describe('TimelineConfiguration', () => {
  it('should create an instance', () => {
    expect(new TimelineConfiguration()).toBeTruthy();
  });
});
